import pytest
from model_bakery import baker

'''
@pytest.fixture
def segmento(db) -> Segmento:
    model = baker.make(Segmento, _fill_optional=True, deleted_at=None)
    return model


@pytest.fixture
def categoria(db) -> Categoria:
    model = baker.make(Categoria, _fill_optional=True, deleted_at=None)
    return model


@pytest.fixture
def marca(db) -> Marca:
    model = baker.make(Marca, _fill_optional=True, deleted_at=None)
    return model


@pytest.fixture
def produto(db) -> Produto:
    model = baker.make(Produto, _fill_optional=True, deleted_at=None)
    return model
'''
